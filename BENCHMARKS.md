# TRZK Benchmarks (3.14.0)

## Criteria (3.14.0)

Rubric generated by `/plan-project` for v3.14.0.

### Correctness
<!-- CHECK:N314.1:butterflyCost(useShift=true, staticShift=false) includes branchCheck penalty -->
<!-- CHECK:N314.6:Python four-step matches naive DFT 100% for N=8,64,128,1024 -->
<!-- CHECK:N314.9:Four-step C compiles with cc -O2 and matches Python reference -->
<!-- CHECK:N314.12:NodeOps MatOp instance all 4 laws pass (mapChildren_children, mapChildren_id, replaceChildren_children, replaceChildren_sameShape) -->
<!-- CHECK:N314.14:applyBreakdownInEGraph correctly injects CT factorization into EGraph MatOp -->
<!-- CHECK:N314.15:E-graph extracted plan competes in selectPlanWith without errors -->
- 0 new sorry in any file
- BabyBear ±3% vs v3.12.0 (no regression)
- Python four-step reference matches naive DFT (100% for all test sizes)
- Four-step C output matches Python reference (all sizes)
- selectBestPlan NEVER picks plan worse than CT standard
- MatOp NodeOps instance compiles with all 4 laws

### Performance
<!-- CHECK:N314.11:Goldilocks gap ≤ 0.85x post four-step -->
<!-- CHECK:N314.5:feedback_loop.py prediction matches benchmark ±20% -->
- Goldilocks gap ≤ 0.85x post four-step (target)
- feedback_loop.py prediction matches benchmark ±20%
- BabyBear ±3% vs v3.12.0

### Quality
<!-- CHECK:N314.all:No modules-isla (wiring_check.py PASS) -->
<!-- CHECK:N314.5:Feedback loop runs for all new candidates -->
<!-- CHECK:N314.15:E-graph plan competes in selectPlanWith -->
- No modules-isla (wiring_check.py PASS)
- Feedback loop mandatory for new candidates
- E-graph plan competes in selectPlanWith
- No import cycles after VerifiedExtraction.Core addition

---

## Current Results (3.14.0)

(no blocks closed yet)

---

## Previous Criteria (3.13.0)

## Criteria (3.13.0)

Rubric generated by `/plan-project` + structural QA during planning.
This section is written ONCE and not modified during execution.

### Correctness
<!-- CHECK:N313.1:reductionCostForHW .lazy = mixedOpCost hw (.reduceGate 0 0), NOT 0 -->
<!-- CHECK:N313.1:costAwareReductionForBound never returns .lazy (no lazy candidates) -->
<!-- CHECK:N313.5:trzk --target c produces C via UltraPipeline Path A -->
<!-- CHECK:N313.6:FRI fold Path A output numerically identical to Path B for all fields -->
<!-- CHECK:N313.7:Horner Path A output numerically identical to Path B for all fields -->
<!-- CHECK:N313.8:Dot product Path A output numerically identical to Path B for all fields -->
<!-- CHECK:N313.9:grep "import.*UnifiedCodeGen" returns 0 active importers after F.2-F.4 -->
<!-- CHECK:N313.12:ILP2 output numerically identical to non-ILP2 for same input -->
<!-- CHECK:N313.13:useShift=false produces identical codegen to v3.12 -->
<!-- CHECK:N313.13:nttDataIndex(offset=0,stride=1) backward compatible with v3.12 -->
<!-- CHECK:N313.14:two_step_ntt == reference_dit_ntt for 4 sizes × 5 inputs (100% match) -->
<!-- CHECK:N313.15:mkTwoStepPlan.wellFormed = true -->
<!-- CHECK:N313.15:planTotalCostWith includes twiddle matrix cost when plan has useShift stages -->
<!-- CHECK:N313.16:lowerTwoStepNTT C compiles with cc -O2 (no type errors) -->
<!-- CHECK:N313.16:two-step NTT output matches standard NTT for goldilocks N=128,256,1024,16384 -->
<!-- CHECK:N313.17:benchmark.py --validation-only --fields goldilocks PASS -->
- 0 new sorry in any file
- BabyBear validation PASS (unchanged output)
- Goldilocks validation PASS for standard AND two-step NTT
- Lazy cost model reverted (no .lazy candidates in costAwareReductionForBound)
- Path A numerically identical to Path B for FRI fold, Horner, dot product
- Two-step Python reference matches standard reference (20/20 tests)
- Two-step C output matches Python reference (all sizes)

### Performance
<!-- CHECK:N313.12:ILP2 >= ILP1 performance for Goldilocks (no regression) -->
<!-- CHECK:N313.17:Goldilocks gap < 0.85x post two-step -->
- Goldilocks gap ≤ 0.85x post Phase H (two-step NTT)
- Goldilocks gap ≤ 0.90x post Phase G (ILP2 only)
- BabyBear ±3% vs v3.12.0 (no regression)
- ILP2 >= ILP1 performance for all sizes

### Quality
<!-- CHECK:N313.3:wiring_check.py PASS after dead code removal -->
<!-- CHECK:N313.9:0 active importers of UnifiedCodeGen after F.5 -->
<!-- CHECK:N313.11:CI GitHub Action runs benchmark validation on every PR -->
<!-- CHECK:N313.all:No modules-isla (wiring_check.py --entry-points Bench.lean PASS) -->
- All primitives use Path A (no Path B string emission in active code)
- Compiler driver wired to UltraPipeline (verified pipeline)
- CI validates correctness on every PR
- No modules-isla (wiring_check.py PASS)
- Net code reduction: ~530 LOC smaller (1321 legacy removed, 790 new added)
- README has Verification Status table

---

## Formal Properties (3.13.0)

Stubs for SlimCheck-based property testing (advisory, not blocking).

```lean
-- N313.1: Lazy cost model reverted
example : reductionCostForHW arm_cortex_a76 .lazy =
          reductionCostForHW arm_cortex_a76 .solinasFold := by native_decide

-- N313.13: Backward compatibility — useShift=false
example : butterflyCost .r2 arm_cortex_a76 false = butterflyCost .r2 arm_cortex_a76 := by rfl

-- N313.15: Two-step plan wellFormed
example : (mkTwoStepPlan 18446744069414584321 (2^14) arm_cortex_a76).wellFormed = true := by
  native_decide
```

---

## Current Results (3.13.0)

(no blocks closed yet)

---

## Previous Criteria (3.12.0)

### Correctness
- 0 new sorry in any file
- BabyBear validation PASS (identical output)
- Goldilocks validation PASS (identical output)
- CacheConfig.elementSize = 8 for k=64 fields

### Performance
- Goldilocks gap ≤ 1.10x post-A.1 (F5c)
- BabyBear ±3% vs v3.11.0 (no regression)
- Discovery plan participates in selectPlanWith (report shows)

### Quality
- No modules-isla (wiring_check.py PASS)
- selectBestPlanExplored used directly (no recreating CostOracle/Plan)

---

## Previous Results

### 3.8.0

## Criteria (3.10.0)

Rubric generated by `/plan-project` + Gemini QA + 4-round adversarial debate.

### Correctness
<!-- CHECK:N310.1:isPow2Twiddle matches manual Python computation for all stages -->
<!-- CHECK:N310.1:Shift butterfly = multiply butterfly numerically for ALL twiddle values -->
<!-- CHECK:N310.1:BabyBear NTT output unchanged (0 pow2 twiddle effect) -->
<!-- CHECK:N310.6:Plan.totalCostWith(reductionCostForHW) = Plan.totalCost (wrapper equivalence) -->
<!-- CHECK:N310.6:lazy_eq_solinas_cost rfl compiles (uses reductionCostForHW, not Plan.totalCost) -->
<!-- CHECK:N310.7:useDynamicCost=false produces identical code to v3.9.0 -->
<!-- CHECK:N310.9:ILP2 output = ILP1 output for all N and inputs -->
<!-- CHECK:N310.2:semanticMatchStep bypassed without affecting NTT output -->
- Goldilocks NTT validates against Python reference for N=2..2^20
- BabyBear/KoalaBear/Mersenne31 unchanged after every block
- 0 new sorry, 0 new MixedNodeOp constructors
- ILP2 numerically identical to ILP1
- useDynamicCost=false identical to v3.9.0

### Performance
<!-- CHECK:N310.1:TA speedup ~25-30% for Goldilocks (40% butterflies skip multiply) -->
<!-- CHECK:N310.9:ILP2 >= ILP1 performance for all sizes -->
<!-- CHECK:N310.all:BabyBear ±3% vs v3.7.0 -->
<!-- CHECK:N310.8:Post-TA+TD gap vs Plonky3 ≤1.1x -->
- TA: ~25-30% Goldilocks speedup
- TD: ILP2 >= ILP1 for all sizes
- BabyBear ±3% vs v3.7.0
- Post-optimization gap vs Plonky3: target ≤1.1x

### Quality
<!-- CHECK:N310.all:0 changes to sacrosanct files -->
<!-- CHECK:N310.all:computeCostsF signature untouched -->
<!-- CHECK:N310.all:reductionCostForHW static preserved -->
<!-- CHECK:N310.all:FRI and Poseidon unaffected -->
- 0 changes to sacrosanct files (VerifiedExtraction, MixedExtract, CostExtraction, MixedNodeOp)
- computeCostsF signature untouched
- reductionCostForHW static preserved (dynamic is opt-in)
- FRI and Poseidon plans unaffected

---

## Results (3.8.0)

(3.10.0)

(no blocks closed yet)

---


### 3.9.0

## Criteria (3.9.0)

Rubric generated by `/benchmark-qa --strict` + Gemini 2-round adversarial QA during planning.
This section is written ONCE and not modified during execution.

### Correctness
<!-- CHECK:N39.1:theorem lowerGoldilocksProductReduce_correct proven (theorem-first mandate) -->
<!-- CHECK:N39.1:k<=32 path generates IDENTICAL code to v3.7.0 (diff verification) -->
<!-- CHECK:N39.2:Goldilocks temps use __int128/i128, BabyBear temps use int64_t/i64 -->
<!-- CHECK:N39.3:benchmark.py --validation-only --fields goldilocks --hardware arm-scalar PASS -->
<!-- CHECK:N39.3:PBT with near-overflow inputs + 128-bit intermediate products -->
<!-- CHECK:N39.5:Nat.mul_sub_left_distrib proof (no sorry) -->
<!-- CHECK:N39.6:Karatsuba decomposition correct mod p (formal theorem) -->
<!-- CHECK:N39.6:E-graph terminates: e-classes < 100k, iterations < 10/expr -->
<!-- CHECK:N39.8:lazy_eq_solinas_cost rfl invariant preserved (dynamic is separate fn) -->
<!-- CHECK:N39.8:useDynamicCost=false produces IDENTICAL results to v3.7.0 -->
<!-- CHECK:N39.9:fromCName roundtrip rfl examples for all 7 new NeonIntrinsics -->
<!-- CHECK:N39.9:goldilocksButterfly4Stmt allCallsKnown = true -->
<!-- CHECK:N39.10:Full pipeline goldilocks arm-neon validation PASS -->
- lowerGoldilocksProductReduce_correct theorem proven BEFORE merge (QA mandate)
- Goldilocks scalar NTT matches Python reference element-by-element (C + Rust)
- BabyBear/KoalaBear/Mersenne31 validation unchanged after every block
- 0 new sorry in any file
- ModularRewriteRule Karatsuba correct mod p (Lean theorem, not just tests)
- E-graph saturation terminates: e-classes < 100k, iterations < 10/expr
- fromCName roundtrip for all 7 new NeonIntrinsics (rfl examples)
- Dynamic cost fallback activates for known fields when P99 error > 5 cycles
- lazy_eq_solinas_cost (NTTPlan.lean:302-303 rfl) preserved
- Golden vector suite: version-controlled NTT input/output pairs per field

### Performance
<!-- CHECK:N39.all:BabyBear benchmarks ±3% vs v3.7.0 after every block -->
<!-- CHECK:N39.4:exprCostHW ratios match measured: mean relative error < 5% -->
<!-- CHECK:N39.4:bench_goldi_isolated 1000+ iterations per variant, std-dev reported -->
<!-- CHECK:N39.8:A/B benchmark BabyBear dynamic >= static-3% -->
<!-- CHECK:N39.10:Goldilocks NEON ~1.3-1.5x vs scalar (2-lane 64-bit) -->
- BabyBear benchmarks ±3% vs v3.7.0 after every block
- exprCostHW ratios match measured ratios: mean relative error < 5%
- Calibration micro-benchmark: 1000+ iterations, std-dev reported
- E-graph saturation within maxNodes budget for all fields
- A/B benchmark: dynamic cost BabyBear >= static-3%
- Goldilocks NEON ~1.3-1.5x vs scalar (realistic for 2-lane 64-bit)

### Quality
<!-- CHECK:N39.all:0 new constructors in MixedNodeOp -->
<!-- CHECK:N39.all:computeCostsF signature unchanged -->
<!-- CHECK:N39.8:reductionCostForHW static preserved (dynamic is opt-in) -->
<!-- CHECK:N39.8:FRI and Poseidon plans unaffected -->
<!-- CHECK:N39.8:discoverAllStages uses cached single saturation -->
<!-- CHECK:N39.9:Trust boundary documented: evalStmt(.call) = none -->
- 0 new constructors in MixedNodeOp (315 branches untouched)
- computeCostsF signature unchanged (extractF_correct safe)
- reductionCostForHW static preserved (dynamic is opt-in via useDynamicCost)
- useDynamicCost=false produces bit-identical results vs v3.7.0
- FRI and Poseidon plans unaffected
- discoverAllStages uses cached single saturation (not N saturations)
- Trust boundary documented for NeonIntrinsic 64-bit (evalStmt(.call) = none)
- No field-specific branches in temp decls — single parametric path by k
- PBT mandate: SlimCheck or Python hypothesis for N39.1, N39.3, N39.9

---

## Current Results (3.9.0)

(no blocks closed yet)

---

## Previous Results

### 3.7.0

## Criteria

Rubric generated by `/benchmark-qa --strict` during planning.
This section is written ONCE and not modified during execution.

### Correctness
- simdStmtToC produces compilable C for all NEON intrinsics
- All butterfly Stmt sequences use NeonIntrinsic names (no raw strings)
- FRIFoldPlan never returns Montgomery for sums
- reductionCost callers migrated (except Phase23Integration olean-only)
- lake build 0 errors, 0 new sorry in new files

### Performance
- Verified SIMD path ±3% vs legacy string emission path
- No scalar regression

### Quality
- NeonIntrinsic ADT is single source of truth for intrinsic names
- Trust boundary documented explicitly
- Structural theorems: operation count + intrinsic names + data flow pattern

---

## Current Results

(no blocks closed yet)

---

## Previous Results

### 3.6.0

## Criteria

Rubric generated by `/benchmark-qa --strict` during planning.
This section is written ONCE and not modified during execution.

### Correctness
- halfSize=2 butterfly output matches scalar for all 3 fields
- halfSize=1 butterfly output matches scalar for all 3 fields
- Full NTT output matches Python reference element-by-element
- lake build 0 errors, 0 new sorry
- output[i] < p for all i (Harvey guarantee)

### Performance
- Stages 14-15 drop from ~128us to ~19-30us each
- Total NTT N=2^16 drops from ~530us to ~310-340us
- No regression in stages 0-13 (SIMD) or scalar path

### Quality
- No code duplication (reuse sqdmulh REDC pattern)
- Small SIMD butterflies use same constant vectors as full butterfly
- emitStageC dispatch is clean 3-way: full SIMD / small SIMD / scalar

---

## Results (3.6.0)

(no blocks closed yet)

---


### 3.5.0

## Criteria

Rubric generated by `/benchmark-qa --strict` during planning.
This section is written ONCE and not modified during execution.

### Correctness
- sqdmulh REDC output in [0,p) for all inputs in [0,p)
- Signed arithmetic contained within REDC
- ilpFactor does not change numerical output
- mu_tw precomputation correct
- lake build 0 errors, 0 new sorry

### Performance
- sqdmulh NEON improves >=30% over vmull NEON
- ilpFactor=2 improves >=10% additional
- Scalar zero regression
- Cost model predicts gain within 20% of actual

### Quality
- No code duplication
- New dimensions discoverable by cost model
- Each dimension has smoke test
- Benchmark vs P3 real documented

---

## Results (3.5.0)

(no blocks closed yet)

---


### 3.4.0

## Criteria

Rubric generated by `/benchmark-qa --strict` during planning.
This section is written ONCE and not modified during execution.

### Correctness
- All 3 Harvey bound sites consistent (boundK=1)
- stage_bound_correct theorem proves .harvey => outputK = 1
- NEON Harvey butterfly output matches scalar Harvey for 3 fields
- selectReductionForBound never returns .montgomery for sum/diff context
- lake build passes with 0 errors, 0 new sorry

### Performance
- mkBoundAwarePlan with arm_neon_simd produces all-Harvey schedule (5/5 stages)
- extractScheduleFromState with arm_neon_simd selects Harvey for all stages
- Benchmark: Ultra NEON at least matches P3 baseline (no regression)

### Quality
- No REDC code duplication (extracted as helper)
- emitStageC dispatches by stage.reduction (not hardcoded bfName)
- All smoke test examples updated and passing

---

## Formal Properties

Executable SlimCheck stubs per node. Generated during planning from the
natural-language properties in ARCHITECTURE.md § Formal Properties.
Written ONCE per version, not modified during execution.

### N34.1 Tighten Harvey bound to boundK=1

```lean
-- P0, SOUNDNESS: Harvey reduction output is strictly less than p
example : BoundProp.boundAfterReduction .harvey = 1 := rfl

-- P0, INVARIANT: Harvey chaining: stageBoundFactor enables next stage Harvey eligibility
example : BoundProp.stageBoundFactor 1 .harvey = 1 := by native_decide

```

### N34.2 SIMD Harvey butterfly kernel + dispatch

```lean
-- P0, EQUIVALENCE: SIMD emitter produces distinct butterfly functions for Harvey vs Solinas
eval (emitSIMDNTTC testPlan .neon 27 15 mu ntt).containsSubstr neon_bf_dit_har

```

### N34.3 Fix selectReductionForBound Montgomery exclusion

```lean
-- P0, SOUNDNESS: selectReductionForBound never returns Montgomery for SIMD butterfly
example : selectReductionForBound 5 true false != .montgomery := by native_decide

```

> **Nota**: Los stubs requieren `import Mathlib.Testing.SlimCheck`.
> Tipos custom necesitan instancias `SampleableExt` + `Shrinkable`.
> Stubs son advisory — no bloquean cierre de nodo, pero se reportan en cobertura.

---

## Results (3.4.0)

(no blocks closed yet)

---


